# Backend Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:3000

# CORS Configuration
# Comma-separated list of allowed origins
# Example: http://localhost:3000,https://medio.example.com,https://app.medio.com
ALLOWED_ORIGINS=http://localhost:3000

# Database Configuration
# Option 1: Connection String (preferred for production)
# Uncomment and set this in production. Commented out to allow fallback to individual DB_* variables in CI.
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Option 2: Individual Settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medio
DB_USER=medio
DB_PASSWORD=your_secure_password_here

# SSL Configuration for Production
DB_SSL_REJECT_UNAUTHORIZED=true
# DB_SSL_CA=your_ca_certificate_here

# JWT Configuration
# IMPORTANT: Generate a secure secret with at least 64 characters
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_64_character_minimum_secure_jwt_secret_here_do_not_use_default

# Cookie Configuration
# IMPORTANT: Generate a secure secret with at least 64 characters
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_SECRET=your_64_character_minimum_secure_cookie_secret_here_do_not_use_default

# Rate Limiting (optional overrides)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# AUTH_RATE_LIMIT_MAX_REQUESTS=5

# Monitoring (optional)
# SENTRY_DSN=your_sentry_dsn_here
# LOG_LEVEL=info

# Video Platform API Keys
# YouTube Data API v3 (required for Add Video via Link feature)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable YouTube Data API v3 for your project
# Free tier quota: 10,000 units/day (100 units per video fetch = ~100 videos/day)
YOUTUBE_API_KEY=your_youtube_api_key_here

# Vimeo API (optional - for Vimeo video metadata fetching)
# Get your access token from: https://developer.vimeo.com/apps
# VIMEO_ACCESS_TOKEN=your_vimeo_access_token_here

# Dailymotion API (optional - no API key required for public videos)
# Public video metadata is available without authentication